// Define CRUD method signatures using Copilot.
// e.g. IEnumerable<Product> GetAll(); Product? GetById(int id); void Add(Product p); void Update(Product p); bool Delete(int id);
namespace ProductService.Services
{
    public interface IProductService { }
}
